import React from 'react';
import Home from '../home';

export default function TabsHome() {
  // Render the real Home component inside the Tabs so the tab bar and Profile tab work
  return <Home />;
}
